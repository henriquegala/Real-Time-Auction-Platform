[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "integration: requires a running redis + db + backend stack (env-gated)",
    "redis: hits a real redis instead of fakeredis",
]
