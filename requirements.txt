# Local development / test requirements (NOT used by the Docker images).
# Runtime deps live in backend/requirements.txt. Install these to run `pytest`.
#
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   pytest -q

# Imported by the realtime package under test
fastapi>=0.115
pydantic>=2.7
sqlalchemy>=2.0
redis>=5.0
PyJWT>=2.8

# Test tooling
pytest>=8.0
pytest-asyncio>=0.23
fakeredis[lua]>=2.23
websockets>=12
